/**
 * Application configuration
 *
 * PRE_LAUNCH_MODE: When true, shows the pre-launch landing page without navigation.
 * Set to false to switch back to the full site with the main landing page.
 */
export const PRE_LAUNCH_MODE = true;
